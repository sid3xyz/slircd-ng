/target/

# Database files
*.db
*.db-journal
*.db-shm
*.db-wal
data/

# Test artifacts
slircd.log
*.log

# External irctest checkout (kept local, not committed)
slirc-irctest/

# Secrets and Keys
*.pem
*.key
*.crt
*.pfx
*.p12

# Environment
.env
.env.*

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# =============================================================================
# PRODUCTION CONFIGURATION - NEVER COMMIT REAL CREDENTIALS
# =============================================================================
# Any config file with real passwords, secrets, or production hostnames
# goes here. Use config.toml as the EXAMPLE with placeholder values only.
#
# Naming convention for local configs:
#   config.*.local.toml  - Local development overrides
#   config.*.prod.toml   - Production configs (NEVER COMMIT)
#   config.<hostname>.toml - Host-specific configs (NEVER COMMIT)
# =============================================================================
# Block all config variants except explicit test configs
config.*.toml
!config.test.toml
!config.password-test.toml

# Backup files that might contain secrets
*.bak
*.backup
*.old

# Deployment scripts with credentials
deploy*.sh
!deploy.example.sh

# SSL/TLS certificates and keys
certs/
ssl/
tls/

# Service files with paths
*.service
!*.service.example
test_confusables_main

# Python virtual environments
.venv/
venv/
.pytest_cache/
__pycache__/
*.pyc

# Test result files (automated test outputs)
*_results.txt
*_output.txt
*_failure.txt
*_debug.txt
*_complete.txt
*.log

# Root-level test scripts (should live in scripts/ or tests/)
/test_*.py
/test_*.rs
/test_*.sql
/register_*.py
/register_*.sh

# Host-specific configs
config.wintermute.toml
